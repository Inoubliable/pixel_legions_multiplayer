<!DOCTYPE html>
<html>
	<head>
		<title>Pixel Legions</title>
		<link rel="stylesheet" href="dist/styles.css">
		<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
		<script src="js/gameOver.js"></script>
	</head>
	<body>
		<div class="place-sentence">You have finished on <span class="place">{{place}}</span>. place</div>

		<ul class="ranking">
			{{#each ranking}}
				<li>{{indexToPlace @index}}. {{this.name}} <span class="prize">{{prizes.[@index]}} <span class="coin"></span></span></li>
			{{/each}}
		</ul>

		<div class="rating-change">
			New rating: {{newRating}} ({{ratingDiff}})
		</div>
		<br>
		<a href="home" class="play-again-btn btn-bluish">Play again</a>
	</body>
</html>